extends ../template/layout

block content
  .row
    .small-12.columns
  .row
    .small-12.columns
      #songs
        each song in songs
          .song
            a(href='/songs/#{song.id}'): img#songArt(src=song.art)
            h5#songTitle= song.title


